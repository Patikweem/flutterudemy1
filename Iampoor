import 'package:flutter/material.dart';

void main() {
  runApp(
    MaterialApp(
      home: Scaffold(
          backgroundColor: Colors.white,
          appBar: AppBar(
            title: Center(child: const Text('I Am Poor')),
            backgroundColor: Colors.deepOrange[900],
          ),
          body: Center(
            child: const Image(image: AssetImage('images/poor.png')),
          )),
    ),
  );
}
